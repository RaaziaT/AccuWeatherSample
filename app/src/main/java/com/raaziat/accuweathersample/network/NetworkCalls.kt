package com.raaziat.accuweathersample.network

import android.content.Context

class NetworkCalls(
    context: Context
) {
    private var context = context
    private var networkInterfaces = ApiFactory.networkInterfaces

}